import random

# PROJETO 1 - JOGO TERMO

PALAVRAS = [
    "negro", "termo", "nobre", "algoz", "afeto", "plena", "pesar", "dengo", "digno",
    "sutil", "vigor", "fazer", "inato", "genro", "coser", "ardil", "corja", "prole",
    "poder", "moral", "torpe", "honra", "muito", "justo", "gozar", "anexo", "etnia",
    "sobre", "tange", "lapso", "expor", "haver", "amigo", "tempo", "dizer", "tenaz",
]

def testa_palavra(teste, termo):
    """
    Retorna True se teste for igual a termo, e False caso contrário.
    Exibe na tela teste, o caractere + abaixo de cada caractere certo,
    o caractere - abaixo de cada caractere certo na posição errada,
    e um espaço em branco nos caracteres errados.

    Argumentos:
    teste (str) - A palavra chutada pelo usuário
    termo (str) - A palavra sorteada

    Retorna: bool - True se teste e termo forem iguais, False caso contrário
    """
    pass

def termo():
    """
    Sorteia uma palavra, lê o input do usuário até um total de 6 tentativas,
    e para cada input verifica se foi igual à palavra sorteada.

    Retorna True se usuário acertou a palavra, e False caso contrário.
    """
    pass

# PROJETO 2 - SIMULADOR DE DADOS

def rola_dados(dado, qtde):
    """
    Rola um determinado número de dados e os imprime lado a lado.

    Argumentos:
    dado (int) - O tamanho do dado.
    qtde (int) - O número de dados a serem rolados.

    Retorna: None
    """
    pass

def valida_dados(info):
    """
    Retorna True se um determinado dado é numérico maior que zero.
    Imprime na tela caso seja um dado inválido.

    Argumento:
    info (str) - A informação que precisa ser validada.

    Retorna: bool - True se o dado for numérico, False caso contrário
    """
    pass

def dados():
    """
    Lê as informações e processa a simulação dos dados.

    Retorna None.
    """
    pass

if __name__ == "__main__":
    termo()
    dados()

